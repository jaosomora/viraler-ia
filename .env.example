# API Key de OpenAI (requerida)
OPENAI_API_KEY=

# API Key de Anthropic (requerida para generación de guiones)
ANTHROPIC_API_KEY=

# JWT Secret para autenticación (requerido en producción)
JWT_SECRET=cambia-esto-en-produccion

# Clave de cifrado para sobres de credenciales (requerido para /admin Sobres).
# Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SECRETS_ENCRYPTION_KEY=

# Magic link login por email (Resend). Sin esta key, el link se imprime en consola.
# Obtén la key en https://resend.com → API Keys
RESEND_API_KEY=
# Remitente del email. Usa onboarding@resend.dev para pruebas;
# en producción, un email de un dominio verificado en Resend (ej: hola@algosentido.com).
# Evita "noreply@" — Resend lo penaliza y bloquea la conversación con el usuario.
MAGIC_LINK_FROM_EMAIL=onboarding@resend.dev
# URL base de la app (opcional, se infiere del request si no se setea).
# Útil en producción tras un proxy: https://tools.algosentido.com
APP_BASE_URL=

# URL pública del MCP server (OAuth + /mcp). Usada en /.well-known/* y en el
# header WWW-Authenticate del endpoint /mcp para que clientes como Claude.ai
# descubran el authorization server. Normalmente igual a APP_BASE_URL.
# Si no se setea, se infiere del request (frágil detrás de proxies).
MCP_BASE_URL=

# Secret para firmar la cookie temporal del flujo OAuth (10 min de vida).
# Genera 32+ bytes random: openssl rand -hex 32
# Si no se setea, cae a JWT_SECRET (funciona pero no es lo ideal).
OAUTH_SESSION_SECRET=

# Puerto del servidor (opcional, por defecto 3000)
PORT=3000

# Modo de entorno (desarrollo/producción)
NODE_ENV=development

# Ruta a FFmpeg (opcional, si no está en el PATH del sistema)
# FFMPEG_PATH=/ruta/a/ffmpeg

# Variables para Docker
# Host para Vite en desarrollo dentro de Docker
VITE_HOST=0.0.0.0

# Variables para despliegue en Render
# INSTAGRAM_COOKIES=contenido_del_archivo_cookies_txt

# Información de depuración (opcional)
# DEBUG=true
